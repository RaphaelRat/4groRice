import 'package:flutter/material.dart';

class PlantationSection extends StatelessWidget {
  const PlantationSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const Text('Index 0: Plantações'),
      ],
    );
  }
}
