import 'package:flutter/material.dart';

class EstimationSection extends StatelessWidget {
  const EstimationSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const Text('Index 1: Cálculos'),
      ],
    );
  }
}
